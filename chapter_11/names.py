'''Names (a.k.a. variables)'''
# It's OK to assign the same name to different
# kinds of objects at different times:

x = 0                   # x bound to an integer object
x = "Hello"             # Now it's a string
x = [1, 2, 3]           # And now it's a list
