'''
- built-in range function:
        ~ produces a series of successively higher integers which can be used as indexes in a for.
- built-in function zip:
        ~ returns a series of parallelitem tuples, which can be used to traverse multiple sequences in a for.
- built-in function enumerate:
        ~ generates both the values and indexes of items in an iterable, so we don’t need to count manually.
- built-in function map
'''

# COUNTER LOOPS!