# arguments == parameters (TRUE)

"Function bodies often contain a return statement"
# def name(arg1, arg2,... argN):
#     ...
#     return value

'def Executes at Runtime'
# Python def is a true executable statement
# Because funcition definition happens at runtime, 
# there's nothing special about the function name.
# What's import in the object to which it refers.

'''functions allow arbitrary attributes to be attached 
    to record information for later use'''
def func(): ...                                                # create function object
func()                                                         # Call object
func.attr = ...                                                # Attach attributes     
